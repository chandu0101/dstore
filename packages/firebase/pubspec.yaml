name: dstore_firebase
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=3.13.0 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  dstore_flutter:
  firebase_auth:
  cloud_firestore:

  meta:
  json_annotation:

dev_dependencies:
  test: ^1.14.4
  grinder:
  build_runner:
  dstore_generator:

dependency_overrides:
  meta:

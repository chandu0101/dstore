name: dstore_firebase
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dstore_flutter:
    path: ../dstore_flutter
  firebase_auth:
  cloud_firestore:

  meta:
  json_annotation:       

dev_dependencies:
  test: ^1.14.4
  grinder:
  build_runner: 1.12.2
  dstore_generator:
    path: ../dstore_generator/

dependency_overrides:
  meta: 